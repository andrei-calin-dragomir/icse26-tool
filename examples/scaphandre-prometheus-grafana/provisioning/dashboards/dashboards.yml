apiVersion: 1

providers:
  - name: 'Example Dashboards'
    orgId: 1
    folder: 'Examples'         # This will be the folder name in the Grafana UI
    type: file
    editable: true
    options:
      path: /var/lib/grafana/dashboards/
